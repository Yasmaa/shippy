name: Accounts Service
on: 
  push:
    branches:
      - main
    # paths:
    #   - 'accountservice/**'

# Environment variables available to all jobs and steps in this workflow
env:
  GITHUB_SHA: ${{ github.sha }}
  REGISTRY_HOSTNAME: docker.io
  DOCKER_USERNAME: yasmaa
  IMAGE_NAME: micro
  PORT: 9091

jobs:
  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest
    environment: development
    steps:

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2


    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{secrets.DOCKERHUB_USERNAME}}
        password: ${{secrets.DOCKERHUB_PASSWROD}}

    - name: Push the image to Dockerhub
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: ${{env.DOCKER_USERNAME}}/${{env.IMAGE_NAME}}:${{github.run_number}
      # run: |
      #   docker image tag $IMAGE_NAME:$BUILD_NUMBER docker.io/$DOCKER_USERNAME/$IMAGE_NAME:${{github.run_number}}
      #   docker image push docker.io/$DOCKER_USERNAME/$IMAGE_NAME:${{github.run_number}}

   
